import { Mesh, PerspectiveCamera, Vector3 } from "three";
export declare function tweenCamAndTarget(camera: PerspectiveCamera, lookAtMesh: Mesh, camTarget: Vector3, lookAtTarget: Vector3, cameraTweenDelay?: number, cameraTweenDuration?: number, lookAtTweenDelay?: number, lookAtTweenDuration?: number): any;
